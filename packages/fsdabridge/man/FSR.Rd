% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FSR.R
\name{FSR}
\alias{FSR}
\title{Forward Search for linear regression (FSR)}
\usage{
FSR(handle, y, X, ...)
}
\arguments{
\item{handle}{Engine handle from \code{\link[=start_engine]{start_engine()}}.}

\item{y}{Response vector. A row or column vector with n elements.}

\item{X}{Predictor matrix (n, p-1). The intercept must be included.}

\item{...}{Name/value options passed straight through to \code{FSR} (e.g.
\code{nsamp}, \code{intercept}, \code{init}, \code{plots}, \code{msg}).}
}
\value{
The \code{FSR} output converted to R (named list), including \code{mdr}
(the minimum deletion residual trajectory, as a two-column step/value
matrix).
}
\description{
Thin wrapper around \code{fsda_call()} for the FSDA \code{FSR} routine. Runs the
forward search from a robust starting subset, monitoring the minimum
deletion residual as units are added, to detect outliers and departures
from the assumed linear model.
}
\examples{
\dontrun{
h = start_engine("FSR")
out = FSR(h, y, X, nsamp = 0, intercept = TRUE, plots = 0)
stop_engine(h)
}
}
