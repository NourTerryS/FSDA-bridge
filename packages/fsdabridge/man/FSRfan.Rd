% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FSRfan.R
\name{FSRfan}
\alias{FSRfan}
\title{Forward Search Fan Plot Score Test (FSRfan)}
\usage{
FSRfan(handle, y, X, ...)
}
\arguments{
\item{handle}{Engine handle from \code{\link[=start_engine]{start_engine()}}.}

\item{y}{Response vector. A row or column vector with n elements.}

\item{X}{Predictor matrix (n, p-1). The intercept must be included.}

\item{...}{Name/value options passed straight through to \code{FSRfan}
(e.g. \code{family}, \code{h}, \code{init}, \code{intercept}).}
}
\value{
The \code{FSRfan} output converted to R (named list).
}
\description{
Thin wrapper around \code{fsda_call()} for the FSDA \code{FSRfan} routine.
Monitors score test statistics for transformation parameters during
the forward search.
}
\examples{
\dontrun{
h = start_engine("FSRfan")
out = FSRfan(h, y, X, family = "BoxCox")
stop_engine(h)
}
}
