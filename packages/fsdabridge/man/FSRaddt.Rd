% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FSRaddt.R
\name{FSRaddt}
\alias{FSRaddt}
\title{Forward Search added t statistics for candidate variables (FSRaddt)}
\usage{
FSRaddt(handle, y, X, ...)
}
\arguments{
\item{handle}{Engine handle from \code{\link[=start_engine]{start_engine()}}.}

\item{y}{Response vector. A row or column vector with n elements.}

\item{X}{Predictor matrix (n, p-1). The intercept must be included.}

\item{...}{Name/value options passed straight through to \code{FSRaddt}
(e.g. \code{nsamp}, \code{intercept}, \code{init}, \code{plots}, \code{msg}).}
}
\value{
The \code{FSRaddt} output converted to R (named list), including
\code{Tdel} (the added-t statistics trajectory, as a step-by-variable
matrix).
}
\description{
Thin wrapper around \code{fsda_call()} for the FSDA \code{FSRaddt} routine.
Monitors, along the forward search, the added-t test statistics for
variables not yet in the model, to assess whether they should be added.
}
\examples{
\dontrun{
h = start_engine("FSRaddt")
out = FSRaddt(h, y, X, nsamp = 0, intercept = TRUE, plots = 0)
stop_engine(h)
}
}
