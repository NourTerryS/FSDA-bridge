% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MMreg.R
\name{MMreg}
\alias{MMreg}
\title{MM estimator of regression coefficients (MMreg)}
\usage{
MMreg(handle, y, X, ...)
}
\arguments{
\item{handle}{Engine handle from \code{\link[=start_engine]{start_engine()}}.}

\item{y}{Response vector. A row or column vector with n elements.}

\item{X}{Predictor matrix (n, p-1). The intercept must be included.}

\item{...}{Name/value options passed straight through to \code{MMreg} (e.g.
\code{Sbeta}, \code{auxscale}, \code{conflev}, \code{plots}, \code{msg}).}
}
\value{
The \code{MMreg} output converted to R (named list), including \code{beta}
(the MM regression coefficient estimates).
}
\description{
Thin wrapper around \code{fsda_call()} for the FSDA \code{MMreg} routine. Computes
MM estimates of regression coefficients, starting from an S estimate of
the residual scale, for a highly robust and asymptotically efficient
fit.
}
\examples{
\dontrun{
h = start_engine("MMreg")
out = MMreg(h, y, X, plots = 0)
stop_engine(h)
}
}
