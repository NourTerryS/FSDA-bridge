% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unibiv.R
\name{unibiv}
\alias{unibiv}
\title{Univariate and Bivariate Outlier Detection (unibiv)}
\usage{
unibiv(handle, Y, ...)
}
\arguments{
\item{handle}{Engine handle from \code{\link[=start_engine]{start_engine()}}.}

\item{Y}{Data matrix (n, v) of observations.}

\item{...}{Additional name/value options passed straight through to
unibiv.}
}
\value{
A matrix \code{fre} with one row per observation, describing how
many times each unit fell outside the robust bivariate ellipses and
its Mahalanobis distance.
}
\description{
Thin wrapper around \code{fsda_call()} for the FSDA \code{unibiv} routine —
detects univariate and bivariate outliers in a multivariate dataset
\code{Y} by counting how often each observation falls outside robust
bivariate ellipses, together with its Mahalanobis distance.
}
\examples{
\dontrun{
h = start_engine("unibiv")
fre = unibiv(h, Y)
stop_engine(h)
}
}
